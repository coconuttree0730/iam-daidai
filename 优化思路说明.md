# 优化思路说明：把人物动作改为单张雪碧图交付

> 背景：当前实现走的是「绿幕视频 + WebGL 实时抠色」（`delivery.selected = chroma-video`）。  
> 希望改回「视频抽帧 → 抠绿 → 打包雪碧图」（`alpha-atlas`）路线，且**保留原像素清晰度**，只通过减少帧数达成。

---

## 一、结论先行

在保持原像素（单格 516×642）的前提下，**单张图集最多容纳 42 帧**，因此 121 帧必须降到 42 帧以内。

推荐两套方案，按优先级排列：

| 方案                    | 帧数 | 单格尺寸           | 画质   | 实现难度 |
| --------------------- | -- | -------------- | ---- | ---- |
| **A. 裁紧主体 + 保留 48 帧** | 48 | 466×586（紧贴主体）  | 近似无损 | 低    |
| **B. 每 3 帧抽 1 帧**     | 41 | 516×642（保持原画布） | 略有损失 | 低    |

**若目标只是"鼠标跟随效果不变"**，方案 A 更优：帧数几乎不损，主体还更大。  
**若必须保持原画布构图（含留白）**，则选方案 B。

---

## 二、硬上限：为什么是 42 帧

浏览器纹理单边上限 4096px，运行时**只接受一张主图集**。单格尺寸直接决定容量：

```
columns = floor(4096 / cell_width)
rows    = floor(4096 / cell_height)
capacity = columns × rows
```

当前画布 516×642 时：

```
columns = floor(4096 / 516) = 7
rows    = floor(4096 / 642) = 6
capacity = 7 × 6 = 42 帧
```

**42 帧是不可逾越的天花板。** 规范明确禁止为了塞进一张图而牺牲已确认的清晰度：

> "不得为了塞进一张图而牺牲已确认的清晰度"  
> "不得偷降清晰度"

因此只能减帧，不能降分辨率。

---

## 三、质量依据：动作真实幅度实测

对 121 帧离线抠图结果逐帧测量（`frames/keyed-v2/`）：

| 指标       | 实测值     | 含义                  |
| -------- | ------- | ------------------- |
| 水平中心位移   | 25.5 px | 动作幅度小，主要靠头部旋转而非空间位移 |
| 垂直中心位移   | 18.6 px | 同上                  |
| 相邻帧差异均值  | 0.00729 | 原片偏"慢动作"，帧间冗余显著     |
| 相邻帧差异中位数 | 0.00226 | 过半帧对几乎无变化           |
| 相邻帧差异最大值 | 0.05083 | 出现在动作转折点            |

**关键判断**：相邻帧平均差异仅 0.00729，说明抽帧空间很大；而重复帧判定阈值为 0.004，抽帧后必须保持在阈值之上，否则会被误判为冗余帧删除。

---

## 四、各抽帧方案实测对比

| 抽帧步长      | 帧数     | 能进一张图  | 相邻差异均值      | 最大跳变   | 相对原片差异放大 |
| --------- | ------ | ------ | ----------- | ------ | -------- |
| 原始        | 121    | ❌ 超限   | 0.00729     | 0.0508 | 1.0×     |
| 每 2 帧     | 61     | ❌ 超 42 | 0.01386     | 0.0522 | 1.9×     |
| **每 3 帧** | **41** | ✅      | **0.01872** | 0.0646 | **2.6×** |
| 每 4 帧     | 31     | ✅      | 0.02365     | 0.0673 | 3.2×     |
| 每 5 帧     | 25     | ✅      | 0.02787     | 0.0667 | 3.8×     |
| 每 6 帧     | 21     | ✅      | 0.03128     | 0.0713 | 4.3×     |

**读表要点：**

1. **每 2 帧（61 帧）不可行** —— 超出 42 帧上限，规则直接否决。
2. **每 3 帧（41 帧）是最佳平衡点** —— 正好卡进容量且留 1 帧余量（可挑最优起始相位，避开头尾不稳帧）。差异 0.0187 仍在阈值 0.004 的 4.7 倍以上，不会被误删。
3. **再往上抽性价比骤降** —— 每 4 帧只省 10 帧，差异却再涨 26%。滑动条从 41 档降到 31 档，手感明显变"跳"，文件体积节省有限。
4. **最大跳变 0.065 无法通过抽样消除** —— 它出现在动作转折点（抬头 → 摸下巴切换处），是动作本身的不连续性，与抽样步长无关。

---

## 五、更优方案：裁紧单格，容量翻倍

实测全片有效包围盒（alpha > 16 的像素范围）：

```
x: 26 – 491
y: 32 – 617
实际尺寸: 466 × 586
```

原画布 516×642 中存在约 **50px 水平 / 56px 垂直的空白留白**。裁紧后容量变化：

| 单格方案        | 尺寸          | 列 × 行     | 容量       |
| ----------- | ----------- | --------- | -------- |
| 当前原画布       | 516×642     | 7 × 6     | 42 帧     |
| 24px 边距     | 514×634     | 7 × 6     | 42 帧     |
| **16px 边距** | **498×618** | **8 × 6** | **48 帧** |
| **8px 边距**  | **482×602** | **8 × 6** | **48 帧** |
| **紧贴主体**    | **466×586** | **8 × 6** | **48 帧** |

**结论**：只要把单格宽度压到 **498px 或以下**，行数不变但**列数从 7 增到 8**，容量从 42 提升到 **48 帧**。

即：**每 2.5 帧抽 1 帧（121 / 48 ≈ 2.52），差异约 0.016，比方案 B 的 41 帧画质更好，帧数还多 7 帧。**

### 视觉效果的实际影响

| 单格          | DPR1 显示 | DPR2 显示（CSS px） |
| ----------- | ------- | --------------- |
| 516×642（当前） | 516×642 | 258×321         |
| 466×586（裁紧） | 466×586 | 233×293         |

裁紧后 CSS 显示尺寸略缩（258→233），但**人物在框内的占比显著变大** —— 当前 258×321 的框里本来就有大量透明空隙，用户视觉上会觉得人物更大、更清晰。

---

## 六、推荐执行步骤

### 若选方案 A（裁紧主体 + 48 帧，推荐）

1. **重算包围盒并裁切** —— 以全片 `alpha > 16` 联合包围盒为准（本次为 466×586），加 8–16px 安全边距
2. **重新过预算门** —— `motion_budget.py --frames 48 --cell 482x602`，确认仍返回 `alpha-atlas`
3. **按 2~3 帧步长抽帧** —— 目标 48 帧，挑选起始相位使动作首尾完整
4. **闭环清理** —— `loop_cleanup.py`，检查首尾接缝（实测首尾差异 0.01636，属于开放动作需确认是否需要闭环）
5. **打包图集** —— `motion_pipeline.py atlas`，`--cell-width 482 --cell-height 602`
6. **切换运行时** —— 从 `chroma-video-renderer.ts` 换成 `createCssSpriteRenderer` / 图集渲染器

### 若选方案 B（保持原画布 + 41 帧）

步骤同上，但：

- 裁切步骤跳过，保持 516×642
- 抽帧步长固定为 3（`frames[::3]`）
- 打包 `--cell-width 516 --cell-height 642`

---

## 七、需要用户确认的决策点

1. **是否需要保留原画布留白？**
   - 若页面版式已按 258×321 定稿 → 选方案 B
   - 若可接受人物占比变化 → 选方案 A（画质更优）
2. **是否为闭环动画？**
   - 实测首尾差异 0.01636，动作"抬头看 → 摸下巴 → 回正"，**疑似回到起点**
   - 若是闭环，可用 `loop_cleanup.py --seam-window` 裁掉尾部停顿
   - 若不是，保持开放时间轴，触边停帧
3. **鼠标跟随的映射方式不变？**
   - 当前为线性映射：光标 X 位置 → 帧序号
   - 改雪碧图后映射逻辑完全一致，无需改动

---

## 八、风险与边界提示

| 风险         | 说明                       | 对策                                      |
| ---------- | ------------------------ | --------------------------------------- |
| 抽帧后差异过小被误删 | 阈值 0.004，方案 A/B 均在 0.01+ | 无需处理                                    |
| 裁切导致人物贴边被切 | 已按全片联合包围盒计算              | 保留 8–16px 边距                            |
| 首尾接缝可见     | 首尾差异 0.01636             | 用 `loop_cleanup.py` 或扩大 `--seam-window` |
| 显示尺寸缩小     | 233×293 vs 258×321       | 可在 CSS 中放大容器，但会引入插值模糊                   |
| 文件体积       | 48 帧 × 482×602 WebP      | 需实测，预估 1.5–3 MB                         |

---

## 九、未改变的事实源

以下四项不受本方案影响，仍以现有文件为准：

1. `source/concept-contract.yaml` —— 用户明确要求（仅 `destination` 需按新尺寸更新）
2. `source/motion-brief.yaml` —— 派生计划（抽帧步长、单格尺寸需同步更新）
3. `build/timeline.json` —— 编译结果（重新编译后覆盖）
4. `build/motion-budget.json` —— 预算结果（帧数与单格变化后必须重跑）

**任何尺寸或帧数调整后，必须重新运行预算，旧报告失效。**
