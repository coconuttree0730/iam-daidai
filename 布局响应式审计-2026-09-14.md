# 全站布局响应式审计报告（第一阶段：深入发现问题）

- **日期**：2026-09-14
- **范围**：`src/styles/*.css`（12 份）、`src/components/*.astro`（16 个）、`src/pages/**`（全部页面）、`src/lib/*.js`（3 个交互脚本）
- **方法**：静态代码分析 + grep 交叉验证（死代码、漏导入、选择器跨文件泄漏）。遵守 2026-09-11 裁定，未运行无头浏览器。
- **性质**：**只发现问题，不做修复**。第二阶段的修复项与顺序待逐项裁定；C 级清理涉及删除，遵守「删除必须先解释」，全部列为建议而非执行。

---

## 结论先行

| 级别 | 数量 | 含义 |
|---|---|---|
| **A 硬缺陷** | 2 | 特定视口下必然破损或整页无版式 |
| **B 结构风险** | 4 | 当前主流视口可用，但已知场景（横屏手机/带地址栏浏览器/窄屏）会失配 |
| **C 维护风险** | 5 | 死代码、失真注释、无单一来源的固定坐标——不改会持续腐烂 |
| **轻微** | 1 | 320px 极窄下轻微挤压，无溢出 |

弹性布局的总体健康度较高：hero.css（1461 行）以 `clamp` 公式推导版式、三档断点 1100/900/640 齐全；`lab.astro` 是 `repeat(auto-fit, minmax(min(100%, 320px), 1fr))` 流式网格的站内范本；文章详情、标签页、联系表单等 20+ 文件经交叉验证无需第二阶段改动。破损点集中在两处：**`/works/` 轨道页的断点空档**与 **`/en/tags/` 的漏导入**。

---

## A 级：硬缺陷（特定视口必然破损）

### A1. `/works/` 卡片双下限造成 641–800px 视口盲区

`src/styles/works-rail.css`：

```css
.sheet { --card-w: clamp(760px, 45vw, 920px); }   /* 760px 下限 */
.card-set { height: clamp(440px, 62vh, 560px); }  /* 440px 下限 */
```

- **宽度**：视口在 641–800px 时（手机窗口分屏、窄平板竖屏、小窗口桌面），45vw 恒小于 760，卡宽被钳到 760px——**卡片比视口还宽**。intro 卡停靠右缘 40px，左缘直接出屏。640px 以下有独立兜底 `min(320px, 78vw)`，**641–800 是断点空档**。
- **高度**：横屏手机（视口高 < 620px）时 `clamp(440px, 62vh, 560px)` 被钳到 440px，叠加顶部/底部固定件后纵向被裁。
- **耦合**：`--card-w` 是单一来源契约——`works-view.js` 用 `offsetWidth` 实测几何，`isMobile = vw < 640` 与 CSS 对齐；改这条钳制必须同时核对 JS 行为。

### A2. `/en/tags/` 整页无版式

`src/pages/en/tags/index.astro` 只导入了 `sheet.css` + `archive-chrome.css`，**漏导入 `tags-index.css`**（中文版 `src/pages/tags/index.astro` 第 10 行有）。英文标签页的纸带、卡片网格、分类区全部裸奔为无样式 DOM。修复成本一行，是全表 ROI 最高的一项。

---

## B 级：结构风险（已知场景失配）

### B3. hero 舞台横屏手机失配，全站无 orientation/height 维度断点

`src/styles/hero.css`：`.stage { height: 100svh; min-height: 620px }`。横屏手机（高 < 620px）时 min-height 撑破 `100svh`，页面出现纵向滚动，底部信息条沉到折叠线下。1100/900/640 三档全是宽度维度——全站没有一个 `@media (orientation: landscape)` 或高度维度断点。

### B4. `.dock` 同名双定义：全局属性泄漏进 works 轨道页 + safe-area 缺失

`.dock` 在 `global.css` 与 `works-rail.css` 各有一份定义。全局的 `flex-direction: column / gap / right / z-index / pointer-events` 会泄漏进轨道页的 dock。目前 works 侧用 `left: 50% + right: 18px + translateX(-50%)`，代数上恰好仍居中（盒心 = 0.5V，与 right 无关）——**是脆弱巧合，不是契约**：任何一侧改动属性集都会当场翻车。次要项：works dock 手机档未消费 `env(safe-area-inset-*)`（Base.astro 也未加 `viewport-fit=cover`，该函数当前全局无效，影响小）。

### B5. 博客横幅人物 `-15px` 不随比例缩放

`src/styles/blog-list.css`：`.hero-band__char { left: calc(56.2012% - 15px) }`。aspect-ratio 锁定只保证百分比层对齐，固定 px 项不缩放——窄视口下 -15px 等效比例翻倍，「头盖住 S」的配准会漂移。轻微，但属同一类「公式里混硬编码」病灶。

### B6. BasicDrawer 桌面档用 `vh`

`src/styles/basic-drawer.css`：桌面档 `max-height: min(88vh, 780px)`。手机档已正确用 `min(88dvh, 100dvh)`，桌面档漏改。带地址栏的平板浏览器（iPad Safari）里抽屉底部会被工具栏遮住约 60–110px。一行修复。

---

## C 级：维护风险（不改会持续腐烂）

| # | 位置 | 问题 |
|---|---|---|
| C7 | `src/components/ScrollStage.astro` | **死代码**（grep 确认无页面导入）。其 `.scroll-figure { height: calc(min(62vh, 440px) * 1.1854) }` 与 hero.css 2026-09-12 已改的 `min(62svh, 440px)` 漂移，注释自称「与 hero.css 逐字对应」**已失真**——留着会误导后来者 |
| C8 | `src/components/ScrollDemo.astro` | 同为死代码 |
| C9 | `src/styles/global.css` | 遗留旧 demo 死规则 `.view / .sprite / .readout / table.facts / .note / .notice`，grep 确认无消费方（hero 的 canvas 类名是 `hero-figure`，仅共享 `data-sprite` 属性钩子）。**按裁定：删除前必须先解释，此处只登记** |
| C10 | `src/styles/global.css` | `.announce__panel` 手机档 `top: 72px` 是固定坐标链，注释自declare「改顶栏要跟着改」——顶栏高度无单一来源 |
| C11 | `src/pages/lab/scroll.astro` | 768px 断点是全站 1100/900/640 契约之外的孤例（该页自成一体，可接受，但应知情） |

---

## 轻微

- `PostCard.astro` / `tags-index.css` 的 `.lead { min-width: 120px }`：320px 视口下挤占正文空间，不溢出。

---

## 有意设计（不计入问题，第二阶段勿"修复"）

以下经确认是**用户裁定的有意设计**，审计中已排除，避免误报：

1. 描边大字两端出血（hero 与多处标题的故意超宽裁切）。
2. 黑条在人物之下的层序（桌面/手机无例外）。
3. `/works/` 的 `100dvh` 锁滚轨道（设计核心，不破）。
4. `lab/scroll.astro` 的 `.scroll-figure` 约 709px 故意超宽，被 stage 裁切。
5. 艺术封面不计入 i18n（hero 大字四件套、`#basic` 姓名恒显）。
6. 帧随动几何与视差几何各归其位（人物矩形 vs 舞台矩形），`qa:follow` 有专门断言防退化。

---

## 健康清单（第二阶段无需动）

`post-detail.css` · `work-detail.css` · `tags-index.css` · `tag-detail.css` · `sheet.css` · `archive-chrome.css` · `notfound.css` · `contact-form.css` · `PageHead.astro` · `ArticleToc.astro`（1101px 栏 + sheet + 640px 全出血，UA dialog 怪癖已处理）· `PostCard.astro` · `Paginator.astro` · `PostActions.astro` · `SiteMark.astro` · `LangSwitch.astro` · `Base.astro`（`--chrome-axis` 实测对齐轴）· `works-view.js` · `demo-driver.js`（resize + ResizeObserver 重测）· `index.astro` / `en/index.astro` · `lab.astro`（流式网格范本）· `lab/framedemo.astro` · `categories/*.astro` · `blog/*.astro` · `404.astro`

---

## 第二阶段建议

### 修复优先级（按 ROI 排序）

1. **A2**：`/en/tags/` 补一行 import——成本最低、收益最大。
2. **A1**：works-rail 双下限改造（641–800 盲区 + 横屏高度）。方向：`--card-w` 下限改为 `min(92vw, 760px)` 类公式并同步核对 `works-view.js` 的 640 判定；`card-set` 高度下限引入 svh/横屏档。**改动前先跑一次现有轨道页行为对照**（此页几何与 JS 强耦合）。
3. **B6**：BasicDrawer 桌面档 `88vh → 88dvh`，一行。
4. **B3**：为 hero（及 works 轨道页）补横屏档：`@media (orientation: landscape) and (max-height: 620px)` 放宽 min-height 或改布局密度。
5. **B4**：works 侧 `.dock` 改名（如 `.rail-dock`）或收窄全局 `.dock` 的属性集，消除双定义；顺带评估 safe-area。
6. **B5**：`-15px` 改为相对量（`calc(56.2012% - 0.4em)` 类比例锚定），需先实测配准容差。
7. **C7–C11**：逐项解释后由用户裁定是否清理/收敛；不主动删。

### 视口验证矩阵（修复后逐档过一遍）

| 视口 | 场景 | 重点 |
|---|---|---|
| 320×568 | 最窄主流（iPhone SE1/老安卓） | A1 以下兜底档、`.lead` 挤压、横向溢出 |
| 360×640 | 主流安卓小屏 | 同上 |
| 390×844 | 主流 iPhone（Safari 地址栏） | B6 抽屉、svh/dvh 差异 |
| **641×360** | **手机横屏** | **A1 盲区 + B3 触发区，全站最危险档** |
| 761–800×任意 | 分屏/窄窗口 | A1 盲区（卡片出屏） |
| 768×1024 | iPad 竖屏 | B6、900–1100 中档布局 |
| 1024×768 | iPad 横屏 | B3 边缘 |
| 1101×800 | 刚过 1100 断点 | 桌面档起点回归 |
| 1920×1080 | 标准桌面 | 基线回归 |

> 若用户明确授权，可另写一次性无头视口扫描脚本做运行时溢出验证（当前裁定禁止主动跑，仅列为可选）。
