# 规格：游乐园 · AI 新闻时间线（LAB-NEWS）

- **日期**：2026-09-15
- **状态**：**已实施；2026-09-15 晚间因合规裁定换源（HN → 内容方自营 RSS），D15/D16/D17 有修订、新增 D41**
- **范围**：游乐场板块新增并列二级页「AI 新闻」；新增离线抓取脚本与 GitHub Actions 定时任务
- **性质**：本文件是**决策固化文档**，不是实施记录。25 项决策已经逐轮质询确认（见文末「决策溯源」）；后续修订以文内「**修订（日期）**」块就地记录，不改写原裁定
- **前置约定**：遵守项目现行裁定 —— 运行时零 fetch、改动后不自动跑无头浏览器、删除必须先解释

---

## Problem Statement

daidai 的游乐场（`/lab/`）目前只有「自己做过的实验与教材」这一类内容，全部是**自产内容**。缺一个**外部信息入口**：AI 领域的变化很快，daidai 关心它，但不想为此维护一个真实时新闻站。

具体的痛点是：

1. **想看 AI 新闻，但不想常驻在某个新闻 Aggregator 上**。第三方聚合站的推荐流与广告干扰大，且不能按自己的审美与筛选口径组织。
2. **想要「可回看」的形态，而不是「刷完即走」的信息流**。希望有一条能横向展开的时间线，按年月定位，几个月前的那条新闻还能找回来——这是档案馆的气质，与「新闻 App」相反。
   > ⚠️ **2026-09-15 澄清**：「横向展开 / 按年月定位」这一条**具体设想**已随版式改版放弃（实测窗口跨度仅 23.3h，横轴上恒为 1 个月，600px 恒空）。
   > 但**诉求的实质是「可回看」而不是「横向」**——纵向时间线 + 中轴 + 日期分组同样满足它，且不受「时间跨度不够长」的制约。见 D42。
3. **英文源读起来有摩擦**，但直接机翻整站会污染自己的内容层（项目已裁定「界面切英文、用户内容保中文」）。
4. **不想为这个功能引入运维负担**。个人站的预算与精力都有限，不能为了一个新闻页去长期维护服务器、数据库、定时器。
5. **不想稀释自己站点的 SEO 权重**——为 30 条抓来的外链做背书不划算。

---

## Solution

在游乐场板块内新增一个与「实验卡片墙」**并列的二级页**，用一个 tab 条把两者绑成一组视觉上的「两个 tab」，但它们在路由上是独立页面。

- **中文侧** `/lab/news/`：**纵向时间线**（⚠️ 2026-09-15 版式改版，见 D42；原「横向时间线」已作废）。一条竖向中轴按**天**分组，>900px 时卡片在中轴两侧左右错位，≤900px 退化为左轴单栏。条目显示**中文标题 + 中文摘要**；原文为英文时追加英文原文对照层。点击条目进入原文外链。
- **英文侧** `/en/lab/news/`：同一条时间线（同一套版式），但**只渲染英文原文，隐藏中文对照层**。

数据由一条**离线内容管线**生产，与项目既有的素材流水线同构：

```
GitHub Actions（每天定时 2 次，可手动触发）
  └─ Node 脚本：抓取 → 规范化 → 去重 → 排序 → 截断 → 翻译 → 写 JSON
       └─ 只在内容真变化时才 commit（bot 身份）
            ↓
Cloudflare Pages 检测到 push → astro build 读静态 JSON
            ↓
访客看到的是死静态 HTML，运行时零 fetch
```

**关键性质：线上不新增任何服务端组件。** 不需要 Worker、不需要 KV、不需要 Cron Trigger。Node 脚本只在 GitHub 的机器上跑，它产出的是**数据文件**，不是运行时代码——这与 `motion/` 下的素材流水线是同一类东西。项目的「运行时零 fetch」契约完好无损。

---

## User Stories

### 作为访客：浏览

1. 作为访客，我想在游乐场里看到「新闻」与「实验」两个 tab，这样我能立刻理解这个板块有两类内容。
2. 作为中文访客，我想在中文页看到「英文原标题 + 中文翻译」并排，这样我既能读到中文，也能核对外文原文的措辞。
3. 作为英文访客，我想在英文页只看到英文原文，这样我不会在一个英文界面里读到中文段落而感到割裂。
4. 作为访客，我想看到一条横向贯穿屏幕的时间线，这样我能一眼看出这些新闻分布在多长的时间跨度里。
5. 作为访客，我想让时间线按年月分组标注，这样我能迅速定位到「今年 6 月」那一段。
6. 作为访客，我想在时间线上直接读到新闻摘要，这样我不必点进每一条才能判断值不值得读。
7. 作为访客，我想点击条目跳到原文，这样我能读到完整内容。
8. 作为访客，我想让外链在新标签打开，这样我不会丢掉当前页面的浏览位置。
9. 作为访客，我想知道每条新闻的来源，这样我能判断它的可信度与性质（社区讨论 vs 官方公告）。
10. 作为访客，我想看到新闻的发布时间，这样我能判断它是否已经过时。
11. 作为访客，我想在时间线上用左右箭头按钮切换时段，这样我不必依赖滚轮技巧。
12. 作为访客，我想用拖拽横向移动时间线，这样我在触屏和鼠标上都能操作。
13. 作为访客，我想让时间线容器横滑、而页面本身仍可纵向滚动，这样我手机上不会「滑不动页面」。
14. 作为访客，我想切换年份查看更早的新闻，这样我能回看历史。
15. 作为访客，我想切换年份时页面不重新加载、不出现加载转圈，这样浏览是连续的。
16. 作为访客，我想在还没有任何数据时看到一个体面的空状态，而不是一片空白或报错。
17. 作为访客，我想让页面遵守系统的「减少动态效果」偏好，这样动画不会让我不适。
18. 作为键盘用户，我想用 Tab 键依次聚焦到每条新闻链接，这样我不必用鼠标。
19. 作为键盘用户，我想用方向键移动时间线，这样我能纯键盘浏览。
20. 作为读屏用户，我想听到每条新闻的可访问名称是「标题 + 来源 + 时间」而不是一长串摘要，这样我能快速扫过列表。
21. 作为读屏用户，我想在时间线的横轴上获得正确的语义结构（分组标题），这样我能按年月跳读。
22. 作为手机访客，我想时间线在窄屏上仍然可用，这样我不是只能看桌面版。

> ⚠️ **2026-09-15 版式改版作废的故事**（横向轨道相关）：**4 / 5 / 11 / 12 / 13 / 19 / 21**。
> 逐条：4「横向贯穿屏幕的时间线」/ 5「按年月分组」/ 11「左右箭头切换时段」/
> 12「拖拽横向移动」/ 13「容器横滑但页面纵滚」/ 19「方向键移动时间线」/
> 21「横轴上的分组语义」。理由与替代实现见 **D42**。
> 未被作废且仍然有效的：6（摘要）/ 7（外链）/ 8（新标签）/ 9（来源）/ 10（时间）/
> 14–18（年份切换、空态、减少动态、键盘 Tab、读屏名称）/ 20 / 22。

### 作为站长 daidai：内容生产

23. 作为站长，我想让新闻自动抓取，这样我不必手动搬运。
24. 作为站长，我想让抓取每天定时跑两次，这样新闻的时效性够用而不浪费额度。
25. 作为站长，我想能手动触发一次抓取，这样在我想立刻更新时不必等定时。
26. 作为站长，我想让脚本自动过滤掉与 AI 无关的条目，这样时间线不掺噪音。
27. 作为站长，我想让脚本只看最近 72 小时内的新闻，这样时间线不混入陈年旧闻。
28. 作为站长，我想让脚本用「热度 × 时间衰减」挑出最值得读的 30 条、并按发布时间倒序排列，这样时间线既覆盖完整 72 小时、又能从新到旧顺着读。
29. 作为站长，我想让脚本对同一条新闻去重，这样同一篇论文不会因为出现在两个源里而占据两个位置。
30. 作为站长，我想让脚本把抓取结果限制在 30 条以内，这样页面不会被撑爆。
31. 作为站长，我想让被挤出的旧新闻自动进入归档，这样历史不会丢失。
32. 作为站长，我想让归档按月切分文件，这样每个文件的体积与 diff 都保持小。
33. 作为站长，我想让归档保留中文翻译，这样以后回看时仍然读得懂。
34. 作为站长，我想让翻译在抓取时一次性完成并落盘，这样归档里的翻译不会被反复重算。
35. 作为站长，我想让抓取用的 API key 只存在于 GitHub Secret 里，这样它永远不会进入公开仓库。
36. 作为站长，我想让被抓取的内容用 bot 身份提交，这样我能一眼区分「机器提交」与「我自己提交」。
37. 作为站长，我想让 commit 信息带明确前缀与日期，这样我在 git log 里能快速定位。
38. 作为站长，我想让脚本在内容没有变化时不产生 commit，这样 git 历史不被空提交污染。
39. 作为站长，我想在抓取失败时保留上一次成功的快照，这样页面宁可显示旧新闻也不会显示空白或错误。
40. 作为站长，我想在抓取连续失败时收到通知，这样我不会在不知情的情况下挂了几周。
41. 作为站长，我想让翻译失败时降级为「只显示英文原文」，这样翻译服务出问题时页面依然可用。
42. 作为站长，我想让脚本对临时故障重试、对永久故障立即放弃，这样我不浪费额度在注定失败的请求上。
43. 作为站长，我想让单批翻译失败不影响其他批次，这样一次局部故障不会让整轮 30 条全部退化为英文。
44. 作为站长，我想让脚本在抓取结果过少时不写文件，这样坏数据不会覆盖好数据。

### 作为维护者：工程约束

45. 作为维护者，我想让新增的页面遵守项目既有的纸面骨架与页头范式，这样它看起来像这个站的一部分。
46. 作为维护者，我想让新增样式放在独立 CSS 文件里且不写 `:global()`，这样运行时注入的 DOM 不会因缺少 Astro cid 而样式失效。
47. 作为维护者，我想让新闻数据以静态 JSON 形式进入构建，这样「运行时零 fetch」契约不被破坏。
48. 作为维护者，我想让抓取脚本的纯逻辑可以被独立测试，这样我不必启动浏览器或连网就能验证数据正确性。
49. 作为维护者，我想让数据 schema 的字段含义被明确记录，这样页面与脚本两侧的契约不会漂移。
50. 作为维护者，我想让时间线的定位时间在有原文发布时间时用发布时间、缺失时回退到抓取时间，这样渲染永远不会因为缺字段而崩。
51. 作为维护者，我想让新增的 i18n 键在中英两袋中完全对称，这样 `satisfies` 的键集合校验不会在构建时炸。
52. 作为维护者，我想让新增路由自动进入 sitemap 与 hreflang，这样我不必手工维护索引。
53. 作为维护者，我想让英文页的 import 深度正确（比中文页多一层），这样构建不会解析失败。
54. 作为维护者，我想让 `NO.04` 编号只留在板块落点页上，这样「编号 = 板块定位符」的既有语义不被稀释。
55. 作为维护者，我想让抓取脚本放在构建期不参与的位置，这样它不会被打包进产物。

---

## Implementation Decisions

### 信息架构

- **D1｜路由拓扑**：新闻页与实验卡片墙是**同级独立路由**，不是同一页内的前端 tab 切换。理由是「同级」是信息架构关系，不需要用「同页」来承载；同页方案会破坏运行时零 fetch 契约，且两个视图无法各自被深链、被分享、被独立索引。
- **D2｜导航构件**：用一个共享的 tab 条组件把两个页面在视觉上绑成一组。两个页面各渲染一份 tab 条并互为链接。中文页与英文页各指向自己语言树内的落点。
- **D3｜编号语义**：板块落点页保留其既有编号 chip；新闻页**不给编号**。理由是既有裁定「编号 = 板块定位符」，二级页不该引入新编号（也给不了，四卡导航的编号空间是 01–04）。
- **D4｜新闻不进入实验条目数据**：新闻是并列的二级板块，不是编号为 `LAB-0x` 的实验条目。既有的条目数据文件不被改动，实验卡墙的构建逻辑不受影响。
- **D5｜英文页对称**：英文侧的板块落点页也渲染 tab 条，指向英文侧新闻页。若只做中文半边，英文用户在英文树内将找不到新闻入口。

### 数据层

- **D6｜双语言字段同存**：每条新闻同时持有英文原文与中文翻译两组字段，**不做单字段按语言切换**。理由：中文页要并排显示两者；英文页只是隐藏中文，而不是没有中文。若做成「按语言单字段」，英文页就无法在未来增加「显示中文对照」的选项。
- **D7｜降级不产生空字段**：翻译失败时，中文字段直接等于英文字段的值，并置一个布尔标记表明本条未经过翻译。页面按该标记决定是否显示「未翻译」提示，**不靠判空来决定**。理由是判空逻辑会在「摘要本来就为空」的条目上误判。
- **D8｜时间字段两枚，一枚可缺**：`publishedAt`（原文发布时间，可为 null）+ `fetchedAt`（抓取时间，必有）。时间线定位取「有 publishedAt 则用之，否则回退 fetchedAt」。这条是渲染不崩的底线。
- **D9｜去重主键**：规范化 URL 的短哈希。规范化 = 去查询参数中的追踪项、去 fragment、统一尾斜杠。去重比对的是**当前窗口 + 已被挤出的近期条目**，不需要单独维护一份「已见 URL」文件——少一个文件少一处漂移。
- **D10｜更新时间戳**：数据文件顶层带一个 `updated` 字段记录最近一次成功抓取的时间，用于页面上的「最后更新」显示与空状态判断。

### 归档与保留策略

- **D11｜滑动窗口 + 归档**：当前窗口恒定 30 条，被挤出的条目滚入归档。
- **D12｜窗口未满时向前合并**：若某轮只抓到少量新条目，窗口用旧条目补足到 30 条，而不是只留当日几条。理由是页面不该出现「今天只有 8 条」的空旷感；且时间线的「当前时段」永远有内容，逻辑更简单。

> ⚠️ **此项曾出现裁定反转，需知悉**：daidai 在第三轮答复中选择了「当日快照」（只留当日条目）。第四轮分析指出该选择与「时间线消费归档 + 当前窗口两份数据」的复杂度冲突，daidai 指示「根据推荐的进行」，故最终采用「向前合并到 30 条」。**此条如果与你的本意不符，请指出——它是本规格中唯一经过反转的决策。**

- **D13｜归档按月切分**：归档文件以年月切分，每个文件是同 schema 的 JSON。理由：按年切会让单个文件的 diff 过大；按月切在保持 diff 小的同时，文件数增长可控（一年 12 个）。
- **D14｜归档保留中文翻译**：归档条目与窗口条目结构完全一致，包含翻译。理由是归档的价值就是「以后还能读懂」，删掉翻译等于让它退化成英文链接表。

### 抓取与筛选

- **D15｜数据源选型**：优先**官方 RSS 与免费无 key 的接口**。理由：零密钥意味着零「key 过期导致整个任务变红」的故障面；结构稳定意味着解析器不会因源站改版而崩。商业新闻 API（需 key、有额度限制）**排除**。
  - **修订（2026-09-15 晚，站长裁定换源）**：源白名单收敛为 **6 个「内容方自营 RSS」**——
    极客公园 / 量子位 / 开源中国 / IT之家 / 爱范儿（`lang: zh`）+ arXiv cs.AI（`lang: en`）。
    三条排除理由，各自独立成立：
    1. **Hacker News 整条移除**。YC 的 ToS 明文禁止对本站内容做 scraping / data mining，
       而 HN 走的是 Algolia 检索接口（不是 RSS）。**这一条与数据多少无关，是渠道许可问题**，
       不能靠「抓得少一点」缓解。随之消失的还有 `heat`（`points + num_comments`）——
       RSS 没有热度信号，「热度 × 时间衰减」入选规则因此不可用（见 D16 修订）。
    2. **排除一切「非内容方自营」的抓取面**：第三方聚合、以及实测**返回 HTML 而非 feed**
       的站点（机器之心 / 36氪 / 品玩）、**502** 的（雷锋网）。判据是「URL 里是 RSS，
       回来的也必须是 feed」——名字里带 rss 不等于它是 RSS。
    3. **排除结构有病的 feed**：时间戳偏离真实时间的（InfoQ 中文 +8h）、`description` 恒为
       7 字导流语的（同站）、`description` 是**整篇正文**的（AWS / MIT——与「不入库全文」
       的既有裁定直接冲突，见 D16 修订与 SCHEMA §8）。
  - **修订后的取舍（明示）**：数据量从「HN 一条源一天几百条」降到 **6 源一轮合计 804 条**
    （arXiv 634 + 中文媒体 170）。站长的优先级裁定是
    **稳定 > 安全 > 合规 > 内容质量**，并明确「即使数据很少也没事」——本修订按此执行。
- **D16｜筛选口径**：关键词白名单粗筛 → 72 小时时间窗 → URL 规范化去重 → 「热度 × 时间衰减」排序 → 截断 30 条。
  - **修订 1（2026-09-15，站长裁定）**：上面这个「排序」只决定**入选**——哪 30 条进窗口，
    **不决定呈现顺序**。窗口内一律按发布时间**倒序**（最新在前）。
  - **理由**：这是「时间线」，从上往下读应当就是从新到旧；而且热度在页面上不可见，
    若再拿它排顺序，读者会看到「68 小时前的新闻排在最上面」却无从解释。
  - **修订 2（2026-09-15 晚，随 D15 换源）**：**「热度 × 时间衰减」已被删除**，入选改为
    **逐源定额**：每源独立过「关键词 + 时间窗」后按时间倒序取前 `perSourceLimit`
    （默认 6，arXiv 4），各源结果合并去重、整体按时间倒序、截前 30。
    - **理由**：RSS **没有热度字段**（D15 修订 1 的直接后果）；而候选池的形状极不均匀——
      arXiv 单源一轮 634 条，五个中文源合计 170 条。若按全局排序，arXiv 会把 30 个位置
      全部吃光；若只看中文源，窗口又会被 IT之家 这类高频源占满（它最新 6 条落在 1.3h 内）。
      逐源定额 = **把 30 个位置摊到不同发布节奏的源上**，是旧热度规则在换源后的等价手段。
    - **实测**：30 条锚点跨度 **23.3 小时**（最旧一条 23.3h 前；各源自身跨度 0–20.6h），
      「时间线」的时间纵深感得以保留。
    - **顺带产出的安全边界**：`summary` 一律**截断到 140 字符**（源站 `description`
      长度谱实测 16 字 ~ 35,283 字，存整篇 = 把外站正文入库分发，与 Out of Scope 第 9 条冲突）。
  - **契约实现**：`scripts/ai-news/SCHEMA.md` §3「入选 vs 顺序」；代码见
    `pipeline.mjs` 的 `orderWindow()`（顺序的唯一实现点）。
- **D17｜不做多源兜底**：单一数据源不可用时，**不切换到备用源**。理由是备用源会引入第二套解析器、第二种时间格式、第二个维护面；且同一网络事件往往同时影响多个源。失败语义是「静默保留上一次快照」+ 通知。
  - **修订（2026-09-15 晚）**：**「多源并存」不等于「多源兜底」**，两者别混。白名单里
    6 个源**各自独立抓取、各自独立降级**（单源失败只丢该源）；本条禁止的是
    「源 A 挂了就改抓源 B 顶替它」——那才会引入第二套解析口径。逐源失败的全部后果
    记在 `lib/source.mjs` 的 `{ok:false, error}` 上，并由 `describeSources()` 打出
    「N/M 源成功 · id:count 失败[id=reason]」，这是排查稳定性问题的一手信息。
- **D18｜静默失败优先于错误展示**：抓取结果少于阈值时**不写文件、以成功退出**，保留上一次快照。核心原则：**宁可展示三天前的新闻，也不展示空白或报错的新闻页。** 代价是静默失败——因此必须有通知兜底（见 D22）。

### 翻译

> **修订（2026-09-15 晚，实施期）：本节全部 4 条当前处于「已实现、默认关闭」状态。**
> `config.mjs` 的 `translate` 默认 `false`，此时 `run.mjs` **完全不构造翻译器、也不读
> `GLM_API_KEY`** —— 默认路径零第三方数据流、零密钥依赖。这与站长的优先级裁定
> （稳定 > 安全 > 合规 > 质量）一致：翻译是唯一会把数据送出本机的环节。
> 换源之后本节还有一处**语义缺口**待裁定：现白名单以中文源为主，而本节描述的方向是
> **en→zh**（把英文原文译成中文）；若要让中文源在英文页也有真英文标题，需要 **zh→en**，
> 那是新增能力而不是打开开关。D41 选了「不开翻译」，这个缺口因此暂不阻塞。

- **D19｜翻译模型与端点**：使用智谱 GLM 的免费档模型（OpenAI 兼容端点）。**免费档的物理约束必须写进预期**：单并发、约 1 请求/秒、生成速度约 20 token/秒。这不是缺陷，是选型代价。
- **D20｜翻译粒度与批量**：翻译**标题 + 摘要**两项（不是只译标题；新闻条目没有「段落」，所以原文所说「段形式翻译」落地为「标题 + 摘要两段式」）。**强制批量**：每次请求打包若干条，避免「每条一次请求」在单并发下把总时长拉到十几分钟。返回格式**必须是严格 JSON 数组**（不要自然语言），否则解析会崩。
- **D21｜重试策略：2 次 + 指数退避 + 差异化**：
  - 限流 / 服务端错误 → 重试 2 次，退避间隔递增
  - **鉴权失败（key 无效等）→ 立即放弃，不重试**，并让整个任务以失败退出（这是配置错误，重试只是浪费）
  - 返回格式不可解析 → 重试 1 次，仍失败则**该批次降级**，其余批次继续
  - **降级粒度是「批次」而不是「整轮」**——一批挂了不能让 30 条全变英文
- **D22｜失败通知**：使用 GitHub 原生的 workflow 失败邮件（零配置、零成本）。不引入额外的告警通道。

### 呈现层

- **D23｜横向时间线用「横滑容器 + 纵向页面」形态**：时间线容器自身横向可滚动，页面本身保持正常的纵向滚动。理由是**手机上的手势冲突是致命的**——若锁定视口高度让滚轮全权驱动横移，用户在手机上就无法上下滚动页面。项目已有同类先例（既有实验页的「手机内滑动」）。
  - **修订（2026-09-15，站长裁定）**：**本决策作废，横向形态整条不存在了**。改版为纵向时间线（见 D42）。
  - **作废的实测依据**：原设计的前提是「一条横轴按年/月铺开」，隐含假设是**时间跨度足够长**。而抓取窗口只有 72h、冷启动快照实测跨度 **23.3h** ⇒ 横向轨道上**永远只有 1 个月**
    = 1 条 320px 宽的月份列，960px 容器里约 **600px 恒为空**（站长截图指出的「大面积空白」即此）；还需按 ‹ › 横向滚动才能看到别的月份。更硬的死法是**归档会生长**：跑几个月后每个月列会变成几百条卡片的超长竖列，横向方案越跑越不能看。
  - **代价**：D23 想守的那条底线（「手机上手势冲突是致命的」）在纵向版式下**自动满足**——页面滚动就是浏览器原生纵向滚动，没有任何容器接管滚轮/触控。整套横向机制（滚动容器、拖拽 + 惯性、‹ › 箭头、←→/Home/End、可聚焦 `role=region`）因此整块删除，`ai-news-timeline.js` 从 229 行缩到只剩年份切换。
- **D42｜纵向时间线形态（2026-09-15 版式改版，站长裁定）**：一条竖向中轴 + 按天分组 + **两套形态由 CSS 媒体查询切换**。DOM 只有一套（`.news-list` > `.news-day` > `h2` + `.news-day__items`），两个页面都不含版式分支。
  - **>900px＝中央轴 + 左右错位两列**（站长原提议「左右交错」，实现时改了两处）：
    ① **错位而非同排对齐**——严格同排时左右两条共享同一水平位置，轴上只能落一个刻度，「09:47 与 02:25 谁更早」就丢了；错位后每条各占自己的刻度，轴由上往下读仍是严格时间序。
    ② **卡片自带时间戳 + 轴上只放刻度点**——同排两条各在一侧，时间不能只依赖轴。
    实现要点：错位用 `margin-top`（**必须**，不能用 `transform`——margin 参与行高计算会把整行撑高、保证同列不相叠；`transform` 不参与布局，下一行卡片会压上来），刻度点/连接线是 `.news-item` 的伪元素。
  - **≤900px＝左轴 + 单栏**：两列并排时每列只剩约 150px、汉字一行 8–10 字，不可读。窄屏只保留「轴靠左」这一半形态。
  - **分组粒度＝天**（原为月）：窗口 72h、实测跨度 23.3h，按月分组恒为 1 组、标题没有信息量。日期标题是**骑在中轴线上的胶囊**，用不透明底色（`var(--c-paper)`，即 `.sheet` 底色）把线「切断」。
  - **已知取舍**：行配对（1 左 2 右）会让「矮卡 + 高卡」同排时左列出现空隙。实测缓解有效——摘要中位 **140 字（= 截断满额）**、21/30 条都是 140 字 ⇒ 卡片高度集中在 ~209px，相邻配对高度差中位 0–39px；再叠 `min-height` 与卡底行 `margin-top:auto` 把残差收敛。
- **D43｜显示时区 = `Asia/Shanghai`（2026-09-15 修正）**：条目的时间显示与**日期切分**都按站点读者时区，不按 UTC。原实现用 `timeZone:'UTC'`，会把一条 `17:47(+08:00)` 发布的国内新闻显示成 `09:47`，并把 `+08:00` 深夜发布的条目切到第二天：「按日期分组」因此直接错一天。分组键与显示格式必须走**同一个时区**（实现用 `en-CA` 的 ISO 形状短日期取「时区内的日键」，避免手算偏移）。
- **D24｜切换年份不发起网络请求**：所有年份的数据都在构建产物里，年份切换只做 DOM 显隐。理由是保住「运行时零 fetch」契约；代价是 HTML 体积随年份增长。
- **D25｜默认只渲染当前时段，往年按年份切换**：避免一次性渲染全部历史条目导致 DOM 数量失控（数年累积可达数千条）。
- **D26｜外链一律 `nofollow`**：抓来的 30 条外链不为它们做权重背书——这是 daidai 明确要求的（「我不想因为外链做背书稀释自己的站点权重」）。同时加 `noopener` 与 `target="_blank"`。
- **D27｜markdown 外链插件不覆盖此页**：项目已有的外链增强插件只作用于 markdown 管线，Astro 模板渲染的外链**不会自动获得处理**，必须手工写在模板上。
- **D28｜条目形态为时间线条目而非卡片流**：新闻没有可靠封面图，且引第三方图片会破坏「零外部依赖」的性质；时间线条目在同等高度下信息密度更高。
- **D29｜空状态必须有**：数据为空时渲染体面的空状态，而不是空白或报错。这条是健壮性底线——任何数据文件被误删时，全站构建不能因此变红。

### i18n

- **D30｜中英键集合严格对称**：新增的界面文案键在中英两袋中必须完全一致，项目的 `satisfies` 校验会在构建期强制这一点。
- **D31｜语言分工边界**：界面控件与页面结构走 `t()` 走双语；**新闻内容本身不走 `t()`**——它是数据，由语言决定显示哪一组字段（中文页显示两组，英文页只显示英文组）。
  - **修订（2026-09-15 晚）**：「中文页显示两组」**只在两种语言确实不同时成立**——单语源下
    `en` 与 `zh` 是同一串，无条件渲染两组等于把每句标题印两遍。落地规则见 D41。
- **D32｜英文页 import 深度多一层**：英文侧页面比中文侧多一层目录，相对路径的深度**不能照抄中文页**。这是项目已记录的复现型坑。
- **D41｜语言门控（新增，2026-09-15 晚站长裁定）**：**英文页只承载「原文为英文」（`lang === 'en'`）的条目，其余跳过不渲染。**
  - **为什么必须有这条**：源白名单改成单语 RSS 后以中文媒体为主（冷启动实测 **26/30 是中文源**）。
    不设门控，英文页就会把 26 条中文标题与摘要渲染进英文界面，**直接违反 ticket 02 已写定的
    验收条款「页面内不出现任何中文字段」**。这是一条**因换源而新出现的**、换源时没被察觉的缺口。
  - **比过的三个选项**：① 本裁定（只留英文原文）——符合既有验收条款，代价是英文侧时间线明显稀疏；
    ② 照常显示 + 给中文条目打「原文为中文」标——保住时间线完整，但要**改写 ticket 02 的验收条款**；
    ③ 开 zh→en 翻译补齐英文标题——两页都完整，但要引入 GLM 密钥与**第三条数据流**
    （把新闻文本发给智谱），与「稳定 > 安全 > 合规 > 质量」的优先级相悖。**站长选 ①。**
  - **这是隐藏不是删除**：中文原文在中文页完整可见，数据层两个语言袋子都还在，随时可反悔。
  - **连带的三处条款**（都在 `SCHEMA.md` §6）：
    - 中文页的「英文原文对照层」只在 `title.en !== title.zh` 时渲染；
    - 中文页的「未翻译」标只在「本条没有中文可读」（`lang === 'zh' || translated === true` 取反）时渲染
      ——按 `translated` 单值判会把 26 条中文源全部误标成「未翻译」；
    - 英文页的空态须分两种成因：数据真为空 → `news.emptyNote`；有数据但全被门控滤掉 → `news.emptyEnOnly`
      （否则「时间线是空的」与紧随其后的「最后更新 {time}」自相矛盾）。

### 自动化

- **D33｜cron 用 UTC 表达**：GitHub Actions 的 `schedule` 是 UTC，不能按本地时间写。
- **D34｜必须显式声明写权限**：workflow 默认令牌是只读的，不显式声明 `contents: write` 会导致 push 失败。
- **D35｜只有内容变化才 commit**：先判断是否有 diff，无变化直接成功退出。理由是避免每天一次空提交污染 git 历史。
  - **修订（2026-09-15 晚，实现期实测发现本条款此前是死代码）**：workflow 的 `git diff` 判定一直有效，
    但**管线侧**从不给它成立的机会——`pipeline.mjs` 每个成功分支都无条件写 `updated: nowIso`，
    顶层字段一变，diff 就恒为非真。实测证据：固定 RSS + 内存 store、同一份源数据跑两次
    只有 `now` 不同 → 条目 id 集合完全相同，两次落盘字节**不同**，唯一差异是 `updated` 一行。
  - **修法**：管线在写盘前比对「将要落盘的 `items`」与上轮 `current.json` 的 `items`，
    逐字节相同则整条不写、保留旧 `updated`。`updated` 的语义因此从「最后抓取时刻」
    变成「**内容最后变化的时刻**」（页面文案「最后更新」对两种语义都成立）。
  - **收益不是「少几个提交」**：窗口每 12h 真在换血（实测约 15 条换入换出），正常情况照写不误。
    收益是**恢复一个信号**——某轮跑出零 diff 从此意味着「管线产出与上轮完全一致」，
    那是异常（源站挂住 / 解析退化了），值得被看见；以前这个信号被 `updated` 抹掉了。
  - 契约实现：`scripts/ai-news/SCHEMA.md` §8 第 4 条；代码见 `pipeline.mjs` 的 `shouldWrite()`；
    断言见 `qa/ai-news-pipeline.test.mjs` 第 18 / 23 / 24 / 25 项。
  - **新增状态码**：走节流路径时管线返回 `status: 'unchanged'`（**不是** `'skipped'`——
    后者只表示「数据不足 / 全源失败」），`run.mjs` 以退出码 0 收尾。
- **D36｜密钥只存 GitHub Secret**：不进任何配置文件、不进仓库。因为方案是「纯 Action」，Cloudflare 侧不参与，所以不存在密钥需要下发到 Worker 的问题。
- **D37｜已知的定时任务休眠风险**：仓库连续较长时间无活动时，GitHub 会停用定时任务。缓解：该任务每天自己产生 commit，只要抓取成功就自动续期；同时保留手动触发入口。
  - **修订（2026-09-15 晚，与 D35 修订配套）**：D35 的写盘节流若做成无条件，就会把这条续期机制一并掐掉
    ——源站长期停更时内容一直不变，任务再也不产生 commit，60 天后定时任务被静默停用。
    故节流保留兜底 `forceWriteAfterDays`（**45 天**，= 60 天门槛留 15 天余量）：
    距上次写盘超过它时，即使内容没变也强制写一次。
  - **两条契约因此不再冲突**：D35 管的是「内容没变就别提交」，D37 管的是「别让仓库静默 60 天」，
    45 天兜底同时满足两者。改动其中一条时必须复核另一条。

### 明确不做

- **D38｜不新增 Cloudflare Worker / KV / Cron Trigger**。原需求中提到的 worker 分支在本方案中不存在。
- **D39｜不做历史回填**。冷启动只抓当前窗口。理由是回填大量历史在免费档的翻译速率下会消耗数小时的 runner 时间与大量月度额度，且爬回来的旧新闻缺乏正确的上下文，与档案馆的诚实气质不符。历史靠时间自然生长。
- **D40｜不做自动机器翻译正文**。只译标题与摘要；正文留原文，由读者自行点击。

---

## Testing Decisions

### 测试缝（seam）取一条：抓取脚本的 HTTP 边界

**理由**：这个功能真正的风险是**坏数据静默进仓库**——去重没生效、时间戳解析错、排序反了、降级没降成。这些错误在页面上**看起来是正常的**，只是内容是错的。而页面渲染与交互手感属于 Astro 与浏览器的既有能力，出错概率低一个数量级。

因此测试缝取在**最高处**：脚本的 HTTP 边界。把网络调用做成可注入的形参，测试时喂入写死的响应文本，**一次覆盖整条链路**（解析 → 规范化 → 去重 → 排序 → 截断 → 翻译 → 降级 → 序列化）。一条缝覆盖全部逻辑。

**不建第二条缝**的理由：再往下的缝（构建产物的 HTML、时间线的输入折算）都必须在无头浏览器里跑。那与项目现行裁定「改动后不自动跑无头浏览器」直接冲突，且每次残留 30–150MB 的浏览器 profile、让构建时间翻倍。**若日后确需这两条缝，需要当次明确授权。**

### 什么样的测试算好测试

只测**外部可观察行为**，不测实现细节。具体是：

- **不测**：内部函数名、私有变量、调用了哪个内部辅助函数、日志文本。
- **要测**：给定一段响应文本，输出的数据结构是否符合 schema；给定两条指向同一篇文章的不同 URL，输出是否只剩一条；给定一个缺发布时间的条目，时间线定位字段是否回退正确；给定一个鉴权失败的响应，是否**没有**发生重试；给定一个格式错乱的翻译响应，是否**只有该批**标记为未翻译。

判断标准是：**改动内部实现（换一个解析库、调整函数拆分）时，测试不应变红；改动外部行为（改变去重口径、调整降级粒度）时，测试必须变红。**

### 测试模块与先例

- **被测模块**：抓取脚本中与网络无关的纯逻辑部分，以及「注入假网络响应」后的完整管线。
- **运行方式**：`node --test` 或项目既有的独立断言脚本形态，**不需要浏览器、不需要网络**。
- **先例**：项目 `qa/` 下已有多份同构的纯函数回归脚本（无框架、`node` 直接跑、`assert` 断言、结尾打印一行通过信息）。本功能沿用同一形态，不引入测试框架。

### 不做渲染层与交互层的自动化验证

理由已在「测试缝」一节说明。渲染与交互的验证方式保持项目现状：**由 daidai 自己跑开发服务器亲眼看**。

---

## Out of Scope

1. **访客邮箱订阅 / 邮件推送**。本规格只做「聚合展示」，不做「收集邮箱 + 群发」。理由是收集邮箱一旦发生，就落入个人信息处理的合规义务（明示同意、退订机制、留存策略、数据泄露责任），会把一个演示变成一项长期运营负担。若日后要做，应作为独立规格。
2. **站内订阅自有博客更新**。博客的 RSS 已存在且工作正常，本功能不与之合流。
3. **Cloudflare Worker 侧的任何改动**。本方案不需要 Worker、KV、Cron Trigger、Pages Functions。
4. **历史新闻回填**。见 D39。
5. **全文翻译**。见 D40。
6. **多数据源兜底**。见 D17。
7. **新闻条目的封面图 / 富媒体**。见 D28。
8. **评论、点赞、收藏等社交功能**。
9. **新闻全文的站内缓存**（即把外站正文抓回本地存储）——涉及版权与内容归属问题。
10. **自动分类 / 打标签 / 主题聚类的可视化**。
11. **本功能的英文正文翻译**（即把中文页的说明文字翻成英文）——遵守项目「界面切英文、内容保中文」的既有边界。
12. **为这个功能新增无头浏览器验证脚本**。

---

## Further Notes

### 必须写进实施期预期的三条物理约束

1. **翻译耗时是分钟级的，不是秒级的。** 在免费档的「单并发、约 20 token/秒」约束下，30 条新闻的翻译在批量打包后仍需约 2–3 分钟。**这不是 bug，是免费额度的物理代价。** 冷启动时如果你打算把大批历史一次翻译完，耗时会以小时计，并吃掉可观的月度 runner 额度——这是 D39 不做回填的直接原因。

2. **定时任务会有延迟，且会自动休眠。** 定时触发在高峰期的延迟是常态。仓库长时间无活动时定时任务会被停用，本项目靠「每天自己 commit」自动续期，但若连续多日抓取全部失败则不再续期。

3. **免费额度是厂商的单方面承诺。** 当前状态已实测确认（端点存活、免费档限流与速率已核实），但厂商政策随时可变。**降级设计正是为此**——即使免费档取消，页面仍正常显示英文原文，只是少了中文。

### 数量级参考

- **归档体积**：单个条目（含双语）约 600 字节量级。一年按每月累积估算仍在**百 KB 以内**，与项目现有静态资源（约 30MB 量级）相比可忽略。
- **托管限制**：当前托管方案的免费档允许 2 万文件、单文件 25 MiB。本项目当前产物约 200 个文件、最大单文件约 5.7 MB。**新闻功能对这两项限制的消耗可忽略。**
- **真正的增长压力在 git 仓库而非托管**：归档随年份累积。按月切分把年增长控制在十几个小文件，长期可接受；若未来把每日条目数显著调高，需要重新评估。

### 决策溯源

25 项裁定经三轮质询确认。其中：

- **唯一发生反转的是 D12**（原裁定「当日快照」→ 改为「向前合并到 30 条」）。反转理由与提示见 D12 的警示块。
- **唯一被推翻的用户原始设想是「Cloudflare Worker」分支**。原需求提出「github + cloudflare pages/worker」，质询后确认方案为纯 GitHub Action + 静态构建，Cloudflare 侧零新增。
- **2026-09-15 晚追加两次反转，均已就地记录**：① **数据源**由 HN（Algolia）换成内容方自营 RSS 白名单（D15/D17 修订 + D41）；② **版式**由横向轨道换成纵向时间线（**D23 作废**，D42 落地）。两次都不是「实现没写好」，而是**原始假设被实测推翻**：数据源那次是合规与内容版权（HN 用户自写正文被完整复制），版式那次是「横轴按年月铺开」隐含的长时间跨度假设——窗口只有 72h、实测 23.3h，横轴上恒为 1 个月 = 1 列、600px 恒空。
- **一项用户设想被澄清而非否决**：「归档」被理解为另一套系统，实际它是「旧条目继续保留」的存储策略；用户想要的时间线效果消费的正是归档数据——两者是同一件事的两层，不冲突。
- **三处用户自述被指出内部矛盾**：① 「当日快照」与「历史时间线」的数据来源冲突；② 「不给编号」未说明板块落点页的既有编号去留；③ 「时间线」被描述为横向交互但未指定与视口滚动的关系。三者均已在 D3 / D12 / D23 中落定。

---

## 附录：文件级改动清单（易过时，仅供实施参考）

> ⚠️ 本附录刻意与上文分离：路径随重构很快失效，而上面的决策不会。实施时以决策为准，路径仅供参考。

### 新增

| 类型 | 作用 |
|---|---|
| 抓取脚本 | 纯 Node 离线脚本：抓取 → 筛选 → 翻译 → 写 JSON。不参与构建打包 |
| 定时 workflow | 定时触发 + 手动触发 + 写权限声明 + 变化才提交 |
| 当前窗口数据 | 30 条窗口的静态 JSON（冷启动先提交空骨架） |
| 归档数据 | 按月切分的静态 JSON（冷启动先提交空骨架） |
| 中文新闻页 | 纵向时间线 + tab 条（>900px 中央轴错位 / ≤900px 左轴单栏，见 D42） |
| 英文新闻页 | 同上（只显英文原文） |
| 时间线样式 | 独立 CSS 文件，**不写 `:global()`**；两套形态都在媒体查询里，页面不含版式分支 |
| tab 条组件 | 中英共用，文案走 `t()` |

### 修改

| 类型 | 改动 |
|---|---|
| i18n 字典 | 两袋各增约 10 个键，**键集合必须完全一致** |
| 中文板块落点页 | 插入 tab 条；保留既有编号 chip |
| 英文板块落点页 | 插入 tab 条；**不要顺手把此前刻意删除的描边大字加回来** |

### 不动

- 实验条目数据文件（新闻不占 `LAB-0x` 编号）
- Cloudflare 侧的配置文件与既有 Worker
- 既有的验证脚本集合
